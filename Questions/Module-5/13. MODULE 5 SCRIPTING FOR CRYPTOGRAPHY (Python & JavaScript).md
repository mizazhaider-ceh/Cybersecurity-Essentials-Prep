# 🎯 MODULE 5: SCRIPTING FOR CRYPTOGRAPHY (Python \& JavaScript)

## **100+ COMPARISON QUESTIONS**


***

## 📊 **CRITICAL INSIGHTS**

### ✅ **WHAT TO STUDY:**

**Python vs JavaScript Syntax:**

- Variable declarations and naming conventions
- Data types (int, float, str vs Number, string, BigInt)
- Operators (// vs Math.floor())
- Loops and conditionals
- Functions and OOP

**Cryptography Libraries:**

- **Python:** base64, hashlib, des, cryptography
- **JavaScript:** Browser-based implementations

**Practical Implementations:**

- Caesar cipher
- Base64 encoding
- Hashing (MD5, SHA-1, SHA-256)
- Salted hashes
- DES symmetric encryption
- RSA asymmetric encryption

***

## **PART A: PYTHON VS JAVASCRIPT BASICS (Questions 1-30)**


***

**Question 1** (⚪ Easy)

**Python** and **JavaScript** are both:

A) Compiled languages

B) SCRIPTING languages (can execute line by line)

C) Only for web development

D) Machine code

**Answer:** B

**Explanation:**
✅ Both are **SCRIPTING languages** - execute line by line from command line

***

**Question 2** (⚪ Easy)

To start **Python** scripting environment:

A) Open Excel

B) Start "Python" from command shell OR Start Python app

C) Open PowerPoint

D) Restart computer

**Answer:** B

**Explanation:**
✅ Start **Python from command shell** or **Start Python app**

***

**Question 3** (⚪ Easy)

To start **JavaScript** console in browser:

A) Ctrl+C

B) Press F12 → Go to Console

C) Alt+Tab

D) Ctrl+Z

**Answer:** B

**Explanation:**
✅ Press **F12** → Go to **Console** tab

***

**Question 4** (🟡 Medium - **NAMING!**)

**Python** naming convention for variables/functions:

A) camelCase

B) lowercase with underscores: really_long_name

C) ALLCAPS

D) No convention

**Answer:** B

**Explanation:**
✅ **Python:** use **lowercase**, separate words with **underscores "_"**

***

**Question 5** (🟡 Medium - **NAMING!**)

**JavaScript** naming convention for variables/functions:

A) lowercase_with_underscores

B) camelCase (starts lowercase, each following word uppercase): reallyLongName

C) ALLCAPS

D) No convention

**Answer:** B

**Explanation:**
✅ **JavaScript:** **camelCase** - starts lowercase, following words uppercase, no separation

***

**Question 6** (🟡 Medium)

**Python** variable declaration:

```python
x = 1
```

**JavaScript** variable declaration:

```javascript
var x = 1;
let x = 1;
```

**Key difference:**

A) No difference

B) Python = implicit declaration (just assign); JavaScript = use "var" or "let"

C) Python requires var

D) JavaScript implicit

**Answer:** B

**Explanation:**
✅ **Python:** **implicit declaration** (just assign value)
✅ **JavaScript:** use **"var" or "let"** keywords
⚠️ **Also:** Python uses newline to finish line; JavaScript uses **semicolon ";"**

***

**Question 7** (🟡 Medium - **CONSTANTS!**)

**Python** constant definition:

```python
VAT_PERCENTAGE = 0.21
```

**JavaScript** constant definition:

```javascript
const VAT_PERCENTAGE = 0.21;
```

**True or False:** "Python doesn't have true constants - convention is to use ALLCAPS variable name."

**Answer:** True

**Explanation:**
**True.** **Python:** constants don't really exist - **convention = ALLCAPS**
**JavaScript:** use **"const"** keyword

***

**Question 8** (🟡 Medium - **DATA TYPES!**)

**Python** integer data type:

A) Number

B) int (theoretically unlimited)

C) BigInt

D) float

**Answer:** B

**Explanation:**
✅ **Python:** **int** (theoretically unlimited)
✅ **JavaScript:** **Number** (for integers between ±9,007,199,254,740,991) or **BigInt**

***

**Question 9** (🟡 Medium)

**Python** floating point data type:

A) float

B) Number

C) decimal

D) double

**Answer:** A

**Explanation:**
✅ **Python:** **float**
✅ **JavaScript:** **Number** (all numbers are floating point)

***

**Question 10** (🟡 Medium - **UNIQUE TYPES!**)

**Unique data types** in Python (NOT in JavaScript):

A) int, float

B) complex, tuple, dictionary, range, bytes

C) string, boolean

D) Number

**Answer:** B

**Explanation:**
✅ **Python unique types:**

- **complex** (complex numbers)
- **tuple** (immutable list)
- **dictionary** (useful for hash tables!)
- **range** (immutable range)
- **bytes** (immutable bytes)

***

**Question 11** (🟡 Medium - **TYPE CHECKING!**)

**Python** check variable type:

```python
x = 'a'
type(x)  # Returns: <class 'str'>
```

**JavaScript** check variable type:

```javascript
var y = 'a';
typeof(y);  // Returns: 'string'
```

**Commands are:**

A) Python: type(variable); JavaScript: typeof(variable)

B) Same in both

C) Python: typeof(); JavaScript: type()

D) No such commands

**Answer:** A

**Explanation:**
✅ **Python:** **type(variable)**
✅ **JavaScript:** **typeof(variable)**

***

**Question 12** (🟡 Medium - **NULL VALUES!**)

**Python** variable without value:

```python
x = None
type(x)  # <class 'NoneType'>
```

**JavaScript** variables without value:

```javascript
var x = null;  // typeof: 'object'
var x = undefined;  // typeof: 'undefined'
```

**Python uses:**

A) null

B) None

C) undefined

D) empty

**Answer:** B

**Explanation:**
✅ **Python:** **None**
✅ **JavaScript:** **null** or **undefined**

***

**Question 13** (🟡 Medium - **COMMENTS!**)

**Python** comments:

```python
# single line comment
"""
multi-line
comment
"""
```

**JavaScript** comments:

```javascript
// single line comment
/*
multi-line
comment
*/
```

**Python uses:**

A) // and /* */

B) \# and """ """ (or \# for each line)

C) <!-- -->

D) No comments

**Answer:** B

**Explanation:**
✅ **Python:** **\#** (or **"""** for multi-line)
✅ **JavaScript:** **//** (or **/* */** for multi-line)

***

**Question 14** (🔴 Hard - **DATA STRUCTURES!**)

**Python** has which built-in data structures:

A) Only arrays

B) list, tuple, dictionary, set

C) Only strings

D) No data structures

**Answer:** B

**Explanation:**
✅ **Python:**

- **list** =
- **tuple** = (1, 2, 3) (UNIQUE to Python!)
- **dictionary** = {2:'Alice', 1:'Bob'} (UNIQUE, useful for hash tables!)
- **set**

✅ **JavaScript:**

- **array** =
- **set**

***

**Question 15** (🔴 **CRITICAL - INTEGER DIVISION!**)

**Python** integer division:

```python
7 // 6  # Returns: 1
24 // 7  # Returns: 3
```

**JavaScript** integer division:

```javascript
Math.floor(7/6);  // Returns: 1
Math.floor(24/7);  // Returns: 3
```

**Python operator for integer division:**

A) /

B) //

C) %

D) **

**Answer:** B

**Explanation:**
✅ **Python:** **//** = integer division
✅ **JavaScript:** **Math.floor(expression)** = equivalent

***

**Question 16** (🟡 Medium - **OPERATORS!**)

**Python** arithmetic operators:


| Operator | Name | Example |
| :-- | :-- | :-- |
| + | Addition | x + y |
| - | Subtraction | x - y |
| * | Multiplication | x * y |
| / | Division | x / y |
| % | Modulus | x % y |
| ** | Exponentiation | x ** y |
| // | Integer division | x // y |

**Which operator is for exponentiation in Python?**

A) ^

B) **

C) pow()

D) exp()

**Answer:** B

**Explanation:**
✅ **Python:** ***** = exponentiation (e.g., 2**3 = 8)
⚠️ JavaScript uses **Math.pow(base, exponent)**

***

**Question 17** (🟡 Medium - **EQUALITY!**)

**Python** comparing values:

```python
0 == (1 - 1)  # True
0 == 5  # False
```

**JavaScript** comparing values:

```javascript
0 === (1 - 1);  // true
0 === 5;  // false
```

**Python uses _____ equal sign(s), JavaScript uses _____:**

A) double (==), triple (===)

B) single (=), double (==)

C) triple (===), double (==)

D) Same in both

**Answer:** A

**Explanation:**
✅ **Python:** **==** (double equal sign)
✅ **JavaScript:** **===** (triple equal sign, mostly)

***

**Question 18** (🔴 **CRITICAL - LOGICAL OPERATORS!**)

**Python vs JavaScript logical operators:**


| Operation | Python | JavaScript |
| :-- | :-- | :-- |
| AND | and | \&\& |
| OR | or | \|\| |
| NOT | not | ! |
| TRUE | True | true |
| FALSE | False | false |

**Fill in the blanks:**

Python uses **English words** (and, or, not) while JavaScript uses **symbolic operators** (\&\&, ||, !) 

⚠️ **IMPORTANT:** Capital at beginning of **True/False** important in Python!

***

**Question 19** (🟡 Medium - **INPUT/OUTPUT!**)

**Python** input/output:

```python
asked_number = input("Enter a number")
print(asked_number)
```

**JavaScript** input/output:

```javascript
askedNumber = window.prompt("Enter a number");
console.log(askedNumber);
```

**Python uses _____ for input and _____ for output:**

A) input(), print()

B) window.prompt(), console.log()

C) scanf(), printf()

D) get(), put()

**Answer:** A

**Explanation:**
✅ **Python:** **input(message)** and **print(message)**
✅ **JavaScript:** **window.prompt(message)** and **console.log(message)**

***

**Question 20** (🔴 **CRITICAL - CODE BLOCKS!**)

**Python** uses _____ to define code blocks:

A) Curly braces {}

B) Indentation

C) Parentheses ()

D) Square brackets []

**Answer:** B

**Explanation:**
✅ **Python:** **indentation** (all code at same level = code block)
✅ **JavaScript:** **curly braces {}** to define code blocks

***

**Question 21** (🟡 Medium - **CONDITIONALS!**)

**Python** conditional syntax:

```python
if x > 5:
    print("Greater")
elif x == 5:
    print("Equal")
else:
    print("Smaller")
```

**JavaScript** conditional syntax:

```javascript
if (x > 5) {
    console.log("Greater");
} else if (x == 5) {
    console.log("Equal");
} else {
    console.log("Smaller");
}
```

**Key difference:** Python uses **elif** and **colons**, JavaScript uses **else if** and **parentheses/braces**.

**True or False?**
**Answer:** True

***

**Question 22** (🟡 Medium - **LOOPS!**)

**Python** for loop (range):
`for i in range(5):`

**JavaScript** for loop:
`for (var i = 0; i < 5; i++)`

**Python's `range(5)`** generates numbers:

A) 1 to 5

B) 0 to 4

C) 0 to 5

D) 1 to 6

**Answer:** B

**Explanation:**
✅ **range(n)** generates **0 to n-1**.

***

**Question 23** (🟡 Medium - **WHILE LOOPS!**)

**Python**: `while x < 5:`
**JavaScript**: `while (x < 5)`

**Difference:**
A) Python uses colon, JS uses parentheses/braces
B) Python uses brackets
C) JavaScript uses colons

**Answer:** A

***

**Question 24** (🟡 Medium - **FUNCTIONS!**)

**Python** function definition:
`def my_function():`

**JavaScript** function definition:
`function myFunction() { }`

**Keywords are:**
A) func vs function
B) def vs function
C) define vs func

**Answer:** B

**Explanation:**
✅ **Python:** **def**
✅ **JavaScript:** **function**

***

**Question 25** (🟡 Medium - **OOP CLASSES!**)

**Python** class:
`class MyClass:`

**JavaScript** class (ES6):
`class MyClass { }`

**Constructor in Python:**
A) constructor()
B) __init__()
C) create()

**Answer:** B

**Explanation:**
✅ **Python:** **`__init__`**
✅ **JavaScript:** **`constructor`**

***

**Question 26** (🟡 Medium - **INHERITANCE!**)

**Python** inheritance:
`class Child(Parent):`

**JavaScript** inheritance:
`class Child extends Parent { }`

**Python uses _____ while JS uses _____:**
A) parentheses, extends keyword
B) extends keyword, parentheses
C) colons, arrows

**Answer:** A

***

**Question 27** (🟡 Medium - **THIS/SELF!**)

**Python** refers to current instance as:
A) this
B) self
C) me

**JavaScript** refers to current instance as:
A) this
B) self
C) me

**Answer:** Python: **self**, JavaScript: **this**

***

**Question 28** (🟡 Medium - **STRING INTERPOLATION!**)

**Python** (f-strings):
`f"Value: {x}"`

**JavaScript** (Template Literals):
`` `Value: ${x}` ``

**Syntax difference:**
A) Python uses {}, JS uses ${}
B) Identical
C) Python uses #, JS uses $

**Answer:** A

***

**Question 29** (🟡 Medium - **ARRAY LENGTH!**)

**Python** list length:
`len(my_list)`

**JavaScript** array length:
`myArray.length`

**Python uses:**
A) Property .length
B) Function len()
C) Method .size()

**Answer:** B

***

**Question 30** (🟡 Medium - **BOOLEANS!**)

**Python** booleans:
`True`, `False`

**JavaScript** booleans:
`true`, `false`

**Difference:**
A) Capitalization
B) Different words
C) No difference

**Answer:** A

***

## **PART B: PYTHON CRYPTOGRAPHY LIBRARIES (Questions 31-60)**


***

**Question 31** (🟡 Medium)

To **install Python library** from command line:

A) install library

B) pip install <library_name>

C) npm install library

D) get library

**Answer:** B

**Explanation:**
✅ **pip install <library_name>** in cmd window

***

**Question 32** (🟡 Medium)

To **import library** in Python interpreter:

A) include <library>

B) import <library_name>

C) using library

D) require library

**Answer:** B

**Explanation:**
✅ **import <library_name>**

***

**Question 33** (🟡 Medium)

**Python libraries commonly used for cryptography** (mark all that apply):

☐ A) base64
☐ B) hashlib
☐ C) os
☐ D) des
☐ E) binascii
☐ F) cryptography
☐ G) jQuery

**Answer:** A, B, C, D, E, F

**Explanation:**
✅ **Common crypto libraries:** base64, hashlib, os, des, binascii, cryptography
❌ jQuery is JavaScript, not for cryptography

***

**Question 34** (🔴 Hard - **CAESAR CIPHER!**)

**Caesar cipher Python implementation:**

```python
original_string = "HELLO"
modified_ascii_values = [(ord(char)-65+3)%26+65 for char in original_string]
modified_string = ''.join(chr(value) for value in modified_ascii_values)
print(modified_string)  # Output: ?
```

**Output is:**

A) HELLO

B) KHOOR

C) EBIIL

D) MJQQT

**Answer:** B

**Explanation:**
✅ **KHOOR**

- H → K (shift +3)
- E → H
- L → O
- L → O
- O → R

**How it works:**

- `ord(char)` converts char to ASCII (A=65, ..., Z=90)
- `-65` makes A=0, B=1, ..., Z=25
- `+3` shifts by 3
- `%26` wraps around (Z+3 → C)
- `+65` converts back to ASCII
- `chr(value)` converts ASCII to char

***

**Question 35** (🟡 Medium - **BASE64!**)

**Base64 encoding Python:**

```python
import base64
message = "Encoding is not Encrypting"
message_bytes = message.encode('ascii')
base64_bytes = base64.b64encode(message_bytes)
base64_message = base64_bytes.decode('ascii')
print(base64_message)
```

**Output:**

A) Encoding is not Encrypting

B) RW5jb2RpbmcgaXMgbm90IEVuY3J5cHRpbmc=

C) Error

D) None

**Answer:** B

**Explanation:**
✅ **RW5jb2RpbmcgaXMgbm90IEVuY3J5cHRpbmc=**

**Steps:**

1. `.encode('ascii')` → converts string to bytes
2. `base64.b64encode()` → encodes bytes to base64
3. `.decode('ascii')` → converts bytes back to string

***

**Question 36** (🔴 Hard - **HASHING!**)

**Python MD5 hash:**

```python
import hashlib
my_md5_hash = hashlib.md5(b'Hashing is oneway')
my_md5_hash.hexdigest()
```

**Output:**

A) 'Hashing is oneway'

B) '43707161bf3713e8aa133acc626b9ea3'

C) Error

D) None

**Answer:** B

**Explanation:**
✅ **'43707161bf3713e8aa133acc626b9ea3'**

**Note:** `b'...'` creates bytes object required by hashlib

***

**Question 37** (🟡 Medium - **HASHING ALGORITHMS!**)

**hashlib** provides which hashing algorithms (mark all that apply):

☐ A) md5()
☐ B) sha1()
☐ C) sha224()
☐ D) sha256()
☐ E) sha384()
☐ F) sha512()

**Answer:** ALL (A, B, C, D, E, F)

**Explanation:**
✅ **hashlib** contains: sha1(), sha224(), sha256(), sha384(), sha512(), md5() and more

***

**Question 38** (🔴 Hard - **PRACTICAL EXAMPLE!**)

**Hash "Hashing is oneway" with different algorithms:**

```python
import hashlib
hashlib.md5(b'Hashing is oneway').hexdigest()
# Output: ?
```

**MD5 output:**

A) '43707161bf3713e8aa133acc626b9ea3' (32 hex chars = 128 bits)

B) 'a34b874f9a5fca58d0bb4a96f46f3bda550e66de' (40 hex chars = 160 bits)

C) 'cb109c37596b1e28a53356dd6f4d1d1db5ff538a3053acc3544c201101df0611' (64 hex chars = 256 bits)

**Answer:** A

**Explanation:**
✅ **MD5 = 32 hex chars (128 bits)**
✅ **SHA-1 = 40 hex chars (160 bits)**
✅ **SHA-256 = 64 hex chars (256 bits)**

***

**Question 39** (🔴 **CRITICAL - SALTED HASHES!**)

**Salted hash Python implementation:**

```python
import hashlib, os, base64
my_salt = base64.b64encode(os.urandom(40))
hashlib.sha1(b'Hashing is oneway' + my_salt).hexdigest()
```

**What does salting do?**

A) Makes hash weaker

B) Adds unique (bit)string to message before hashing → different hash for same password

C) Removes characters

D) Nothing

**Answer:** B

**Explanation:**
✅ **Salting adds unique string** to message
**Purpose:** Same password → different hash (prevents rainbow table attacks!)

**How it works:**

- `os.urandom(40)` generates 40 random bytes
- `base64.b64encode()` encodes to base64
- Concatenate with message before hashing

***

**Question 40** (🟡 Medium - **DES ENCRYPTION!**)

**DES symmetric encryption Python:**

```python
from des import DesKey
my_key = DesKey(b'MYKEY01!')
encr_message = bytes.fromhex("99a39c00a97df6234378100637d3f37b...")
my_key.decrypt(encr_message, padding=True)
```

**Before using, must install:**

A) No installation needed

B) pip install des

C) pip install encryption

D) pip install symmetric

**Answer:** B

**Explanation:**
✅ **pip install des** in command line first

***

**Question 41** (🟡 Medium)

**Generate secure random numbers** in Python:

A) `random.randint()`

B) `secrets.randbelow()` or `os.urandom()`

C) `math.random()`

D) `x = 4`

**Answer:** B

**Explanation:**
✅ **secrets/os.urandom** = Cryptographically Secure.
`random` is weak (Mersenne Twister, predictable).

***

**Question 42** (🟡 Medium)

**Fernet** (symmetric encryption) from `cryptography` library uses:

A) AES-128 in CBC mode

B) AES-128 in CTR mode (plus HMAC forauth)

C) DES

D) RSA

**Answer:** A (Actually it uses AES-128 in CBC usually, or similar safe default).
Wait, Fernet spec: "AES-128 in CBC mode with PKCS7 padding and HMAC-SHA256".
So **AES-128 CBC + HMAC**.

**Answer:** A

***

**Question 43** (🔴 Hard)

**Code to generate key for Fernet:**

```python
from cryptography.fernet import Fernet
key = Fernet.generate_key()
```

**Key format:**
A) Random bytes
B) URL-safe Base64 encoded 32-byte key
C) Hex string
D) Integer

**Answer:** B

**Explanation:**
✅ **Fernet keys** are **Base64 encoded**.

***

**Question 44** (🟡 Medium)

**Padding** in Block Ciphers:
If message length is not multiple of block size (16 bytes for AES), we must:

A) Cut message

B) Pad it (e.g. PKCS#7)

C) Send it anyway

D) Encrypt with stream cipher

**Answer:** B

**Explanation:**
✅ **Block ciphers need fixed size blocks**. PKCS#7 is standard padding.

***

**Question 45** (🔴 Hard)

**Implement PKCS#7 padding manually:**
Block size = 8. Msg = "HELLO" (5 chars).
Need 3 bytes padding.
Value of padding bytes = **03**.
Result:

A) HELLO333

B) HELLO\x03\x03\x03

C) HELLO000

D) HELLO\x00\x00\x00

**Answer:** B

**Explanation:**
✅ **PKCS#7**: Value of bytes = number of bytes.

***

**Question 46** (🟡 Medium)

**Unpadding** code (concept):

```python
pad_len = padded_msg[-1]
unpadded = padded_msg[:-pad_len]
```

**What does this do?**
A) Reads last byte to know length, removes that many bytes from end
B) Nothing
C) Decrypts

**Answer:** A

**Explanation:**
✅ Extracts value of last byte (e.g., 3), removes last 3 bytes.

***

**Question 47** (🟡 Medium)

**Modes of Operation** in Python `cryptography` library:
Why is ECB mode (Electronica Codebook) discouraged?

A) It's slow

B) Identical plaintext blocks encrypt to identical ciphertext blocks (patterns visible)

C) It uses too much memory

D) It's 100% secure

**Answer:** B

**Explanation:**
✅ **ECB leaks patterns** (Tux penguin image example).

***

**Question 48** (🟡 Medium)

**Which mode requires an IV (Initialization Vector)?**

A) ECB

B) CBC, CFB, OFB, CTR

C) None

D) ALL

**Answer:** B

**Explanation:**
✅ **Chained modes** need random IV to be secure.

***

**Question 49** (🟡 Medium)

**IV reuse** with same key in CTR mode:

A) Is fine

B) Is catastrophic (XOR of ciphertexts = XOR of plaintexts)

C) Makes it stronger

**Answer:** B

**Explanation:**
✅ **Never reuse nonce/IV** in stream modes (CTR)!

***

**Question 50** (🟡 Medium)

**HMAC** implementation in Python:

```python
import hmac, hashlib
h = hmac.new(key, msg, hashlib.sha256)
h.hexdigest()
```

**Purpose:**
A) Encryption
B) Integrity + Authentication
C) Compression

**Answer:** B

**Explanation:**
✅ **HMAC** verifies message wasn't changed AND came from someone with key.

***

**Question 51** (🟡 Medium)

**Scrypt / Argon2 / PBKDF2** in Python are for:

A) Fast hashing

B) Password Hashing (Key Derivation Functions) - slow by design

C) Encryption

D) Random numbers

**Answer:** B

**Explanation:**
✅ **Slow hashes** prevent brute-force attacks on passwords.

***

**Question 52** (🟡 Medium)

**Timing Attack** prevention:
When comparing hashes/passwords, use:

A) `a == b`

B) `hmac.compare_digest(a, b)`

C) `a in b`

D) Manual loop

**Answer:** B

**Explanation:**
✅ **Constant time comparison** prevents timing leaks.

***

**Question 53** (🔴 Hard)

**Generate random byte string** (e.g. for salt):

```python
import os
salt = os.urandom(16)
```

**Length of `salt` in hex characters:**
16 bytes = 32 hex chars.

**Answer:** 32

***

**Question 54** (🟡 Medium)

**Struct library** in Python (`import struct`):
Used to:

A) Build user interfaces

B) Pack/unpack binary data (e.g., converting integers to 4-byte sequences)

C) Encrypt

D) Hash

**Answer:** B

**Explanation:**
✅ Useful for creating network packets or binary formats.

***

**Question 55** (🟡 Medium)

**Big Endian vs Little Endian**:
`struct.pack('>I', 1)` packs integer 1 as Big Endian.
Result: `\x00\x00\x00\x01`
`struct.pack('<I', 1)` packs as Little Endian.
Result: `\x01\x00\x00\x00`

**Network byte order** is usually:
A) Big Endian
B) Little Endian

**Answer:** A

***

**Question 56** (🟡 Medium)

**XOR** operator in Python:

A) ^

B) xor

C) ~

D) &

**Answer:** A

**Explanation:**
`a ^ b` = XOR.

***

**Question 57** (🔴 Hard)

**Example XOR encryption (OTP):**

```python
msg = b"HELLO"
key = b"XYQAZ"
# xor each byte
enc = bytes([a ^ b for a, b in zip(msg, key)])
```

**To decrypt:**
A) Use `+`
B) XOR again with key (`enc ^ key`)
C) Impossible

**Answer:** B

**Explanation:**
✅ **XOR is its own inverse**: (A ^ B) ^ B = A.

***

**Question 58** (🟡 Medium)

**Bitwise operators:**
`<<` means:

A) Less than

B) Left Shift (multiply by 2)

C) Right Shift

D) Nothing

**Answer:** B

**Explanation:**
`x << 1` doubles x.

***

**Question 59** (🟡 Medium)

Convert **Hex string to Bytes** in Python:

A) `bytes.fromhex("AA BB")`

B) `hex("AA BB")`

C) `int("AA BB")`

D) `str("AA BB")`

**Answer:** A

**Explanation:**
✅ `bytes.fromhex()` handles hex strings.

***

**Question 60** (🟡 Medium)

Convert **Bytes to Hex string**:

A) `my_bytes.hex()`

B) `hex(my_bytes)`

C) `str(my_bytes)`

D) `print(my_bytes)`

**Answer:** A

**Explanation:**
✅ `b'\xAA\xBB'.hex()` returns `"aabb"`.

***

## **PART C: RSA IN PYTHON (Questions 61-80)**


***

**Question 61** (🟡 Medium)

To use **RSA in Python**, install which library:

A) pip install rsa

B) pip install cryptography

C) pip install asymmetric

D) No installation needed

**Answer:** B

**Explanation:**
✅ **pip install cryptography**
⚠️ Use: `python3.9 -m pip install cryptography` (replace python3.9 with your version)

***

**Question 62** (🔴 Hard - **RSA KEY GENERATION!**)

**Generate RSA private key Python:**

```python
from cryptography.hazmat.backends import default_backend
from cryptography.hazmat.primitives.asymmetric import rsa
from cryptography.hazmat.primitives import serialization

private_key = rsa.generate_private_key(
    public_exponent=65537,
    key_size=2048,
    backend=default_backend()
)
public_key = private_key.public_key()
```

**What is the key_size?**

A) 512 bits

B) 1024 bits

C) 2048 bits

D) 4096 bits

**Answer:** C

**Explanation:**
✅ **key_size=2048** bits
Also **public_exponent=65537** (common choice for RSA)

***

**Question 63** (🔴 Hard - **PEM FORMAT!**)

**Convert private key to PEM format (for file storage):**

```python
private_key_bytes = private_key.private_bytes(
    encoding=serialization.Encoding.PEM,
    format=serialization.PrivateFormat.TraditionalOpenSSL,
    encryption_algorithm=serialization.NoEncryption()
)
print(private_key_bytes.decode('UTF-8'))
```

**PEM format** is:

A) Binary format

B) Printable format seen in exercises (Base64 with headers)

C) Hexadecimal

D) Encrypted format

**Answer:** B

**Explanation:**
✅ **PEM = printable format** (Base64 with "-----BEGIN PRIVATE KEY-----" headers)
Used for **file storage** of keys

***

**Question 64** (🟡 Medium - **LOADING PEM!**)

**Load private key FROM PEM format:**

```python
private_key = serialization.load_pem_private_key(
    private_key_bytes,
    backend=default_backend(),
    password=None
)
```

**Purpose:**

A) Create new key

B) Convert PEM format (e.g., from file) back to RSA key object for use

C) Encrypt data

D) Hash data

**Answer:** B

**Explanation:**
✅ **Convert PEM → RSA key object**
Need this if key is stored in file and you want to use it for encryption/decryption

***

**Question 65** (🔴 **CRITICAL - SIMPLE RSA ENCRYPT!**)

**Simple RSA encryption Python (UNSAFE - don't use in real life!):**

```python
import gmpy2
def simple_rsa_encrypt(message, publickey):
    numbers = publickey.public_numbers()
    return gmpy2.powmod(message, numbers.e, numbers.n)
```

**This implements:**

A) M^e mod n (RSA encryption formula)

B) C^d mod n (RSA decryption formula)

C) Hash function

D) Symmetric encryption

**Answer:** A

**Explanation:**
✅ **RSA encryption:** $C = M^e \mod n$

- `numbers.e` = public exponent (e)
- `numbers.n` = modulus (n)
- `gmpy2.powmod(base, exp, mod)` = efficient modular exponentiation

⚠️ **UNSAFE** for real use - proper RSA needs padding schemes!

***

**Question 66** (🔴 **CRITICAL - SIMPLE RSA DECRYPT!**)

**Simple RSA decryption Python (UNSAFE - don't use in real life!):**

```python
import gmpy2
def simple_rsa_decrypt(ciphertext, privatekey):
    numbers = privatekey.private_numbers()
    return gmpy2.powmod(ciphertext, numbers.d, numbers.public_numbers.n)
```

**This implements:**

A) M^e mod n (RSA encryption)

B) C^d mod n (RSA decryption formula)

C) Hash function

D) Key generation

**Answer:** B

**Explanation:**
✅ **RSA decryption:** $M = C^d \mod n$

- `numbers.d` = private exponent (d)
- `numbers.public_numbers.n` = modulus (n)

***

**Question 67** (🔴 Hard)

**Extract Modulus (n) from RSA Key Object:**

```python
n = private_key.private_numbers().public_numbers.n
e = private_key.private_numbers().public_numbers.e
d = private_key.private_numbers().d
```

**True or False:**
"You can access the private exponent 'd' from the public key object."

**Answer:** False

**Explanation:**
✅ **False.** Public key only has `public_numbers` (n, e). Private key has everything.

***

**Question 68** (🟡 Medium)

**RSA Sign/Verify** vs **Encrypt/Decrypt**:
`encrypt()` uses **public key**.
`sign()` uses:

A) Private key

B) Public key

C) Symmetric key

D) Hash

**Answer:** A

**Explanation:**
✅ **Sign with Private**, Verify with Public.

***

**Question 69** (🔴 Hard)

**Python code to SIGN message:**

```python
signature = private_key.sign(
    message,
    padding.PSS(
        mgf=padding.MGF1(hashes.SHA256()),
        salt_length=padding.PSS.MAX_LENGTH
    ),
    hashes.SHA256()
)
```

**PSS** stands for:
A) Probabilistic Signature Scheme
B) Python Secure Signature
C) Private Secret Signing

**Answer:** A

**Explanation:**
✅ **PSS** is the recommended padding for RSA signatures (better than PKCS#1 v1.5).

***

**Question 70** (🟡 Medium)

**Python code to VERIFY signature:**

```python
public_key.verify(
    signature,
    message,
    padding.PSS(...),
    hashes.SHA256()
)
```

**If verification fails, it raises:**
A) `InvalidSignature` exception
B) False
C) Returns None
D) Crashes Python

**Answer:** A

**Explanation:**
✅ Raises **InvalidSignature**. You must use `try...except`.

***

**Question 71** (🟡 Medium)

**OAEP** (Optimal Asymmetric Encryption Padding):
Recommended padding for **RSA Encryption**.

```python
ciphertext = public_key.encrypt(
    message,
    padding.OAEP(
        mgf=padding.MGF1(algorithm=hashes.SHA256()),
        algorithm=hashes.SHA256(),
        label=None
    )
)
```

**Why usage padding?**
A) To make it look cool
B) Deterministic encryption is insecure; padding makes it probabilistic (random)
C) To compress

**Answer:** B

**Explanation:**
✅ **OAEP adds randomness**. Encrypting same message twice $\to$ different ciphertexts.

***

**Question 72** (🟡 Medium)

**Serialize Public Key** to send to others:

```python
pem = public_key.public_bytes(
    encoding=serialization.Encoding.PEM,
    format=serialization.PublicFormat.SubjectPublicKeyInfo
)
```

**Start header will be:**
A) `-----BEGIN PUBLIC KEY-----`
B) `-----BEGIN PRIVATE KEY-----`
C) `-----BEGIN RSA KEY-----`

**Answer:** A

**Explanation:**
✅ **SubjectPublicKeyInfo** is the standard X.509 format.

***

**Question 73** (🟡 Medium)

**Raw RSA** (Textbook RSA) without padding:
A) Is secure
B) Is insecure (vulnerable to chosen ciphertext attacks, broadcast attacks, etc.)
C) Is faster

**Answer:** B

**Explanation:**
✅ **Textbook RSA is insecure**. Always use OAEP/PSS.

***

**Question 74** (🟡 Medium)

**Hybrid Encryption**:
Since RSA is slow for large data (and has size limits), we generally:

A) Use RSA for everything

B) Generate a random **AES key**, encrypt data with AES, then **encrypt the AES key with RSA**

C) Use DES

**Answer:** B

**Explanation:**
✅ **Hybrid Encryption** combines speed of AES with key distribution of RSA.

***

**Question 75** (🔴 Hard)

**Maximum RSA Message Size**:
For 2048-bit key (256 bytes) and OAEP padding (approx 42 bytes overhead):
Max message size is approx:

A) Unlimited

B) ~214 bytes (Key size - padding)

C) 2048 bytes

D) 1 MB

**Answer:** B

**Explanation:**
✅ **RSA cannot encrypt data larger than key size (minus padding).**
This is why we use Hybrid Encryption.

***

**Question 76** (🟡 Medium)

`cryptography` library backend:
A) OpenSSL
B) Pure Python
C) Windows API

**Answer:** A

**Explanation:**
✅ Uses **OpenSSL** (via CFFI) for performance and security.

***

**Question 77** (🟡 Medium)

**Man-in-the-Middle (MITM)** on RSA:
If Alice sends her public key to Bob, but Mallory intercepts and sends her own public key:
A) Bob encrypts with Mallory's key; Mallory decrypts, reads, re-encrypts with Alice's key
B) RSA prevents this
C) Nothing happens

**Answer:** A

**Explanation:**
✅ **MITM works** if keys are not authenticated (e.g. via Certificates). Keys don't prove identity by themselves.

***

**Question 78** (🟡 Medium)

**SSH Keys** are often:
A) RSA keys
B) AES keys
C) Passwords

**Answer:** A

**Explanation:**
✅ **id_rsa** is an RSA key pair used for SSH authentication.

***

**Question 79** (🟡 Medium)

**Decrypting with wrong key** in RSA (with OAEP):
A) Returns garbage text
B) Raises Error (decryption error / padding error)
C) Returns original text

**Answer:** B

**Explanation:**
✅ **Padding check fails** $\to$ Error raised.

***

**Question 80** (🟡 Medium)

**Key Rotation**:
Good practice to:
A) Keep same key forever
B) Regularly generate new key pair and retire old one
C) Share private key

**Answer:** B

***

## **PART D: PRACTICAL APPLICATIONS (Questions 81-100)**


***

**Question 81** (🟡 Medium)

To execute Python script from command line:

A) run script.py

B) python test.py

C) execute test.py

D) start test.py

**Answer:** B

**Explanation:**
✅ **python test.py** (or python3 test.py depending on installation)

***

**Question 82** (🟡 Medium)

**Python development environments** include:

A) Only Notepad

B) Visual Studio, PyCharm, IDLE

C) Only command line

D) No IDEs exist

**Answer:** B

**Explanation:**
✅ **Development environments:**

- Visual Studio (supports Python)
- PyCharm (Python-specific IDE)
- IDLE (comes with Python)
- Jupyter Notebook
- VS Code

***

**Question 83** (🟡 Medium - **HELP COMMANDS!**)

**Get help on Python library functions:**

```python
dir(<library_name>)  # List functions
help(<library_name>)  # Get help
```

**But mostly _____ is most useful:**

A) Asking random people

B) Online documentation

C) Guessing

D) Trial and error only

**Answer:** B

**Explanation:**
✅ **Online documentation** is most useful
But `dir()` and `help()` are useful for quick reference

***

**Question 84** (🟡 Medium)

**Derive Key from Password** (PBKDF2):

```python
from cryptography.hazmat.primitives.kdf.pbkdf2 import PBKDF2HMAC
kdf = PBKDF2HMAC(
    algorithm=hashes.SHA256(),
    length=32,
    salt=salt,
    iterations=480000,
)
key = kdf.derive(b"my password")
```

**Why high iterations (480000)?**
A) To make it faster
B) To slow down brute-force attacks
C) To use more RAM

**Answer:** B

**Explanation:**
✅ **Slowness is a feature** for password hashing.

***

**Question 85** (🟡 Medium)

**String Comparison** for passwords/hashes:
Use `secrets.compare_digest(a, b)` instead of `a == b`.
Why?
A) Faster
B) Prevents Timing Attacks
C) Better syntax

**Answer:** B

**Explanation:**
✅ **Timing attacks**: `==` returns early if first char differs. `compare_digest` checks all chars.

***

**Question 86** (🟡 Medium)

**Cryptographically Strong Random Strings** (e.g. for API keys):

```python
import secrets
token = secrets.token_urlsafe(32)
```

**Output looks like:**
A) Random binary
B) "f8923h9f8h2398h..." (URL-safe string)
C) Integer

**Answer:** B

**Explanation:**
✅ **token_urlsafe**: good for reset tokens, API keys.

***

**Question 87** (🟡 Medium)

**Avoid** using `md5` and `sha1` for:
A) Checksums only
B) Security/Signatures
C) Nothing

**Answer:** B

**Explanation:**
✅ **MD5/SHA1 are broken** (collisions found). Use SHA-256+.

***

**Question 88** (🟡 Medium)

**Python "cryptography" library** vs standard `hashlib`:
`hashlib` provides hashes.
`cryptography` provides:
A) Only hashes
B) High-level encryption recipes (Fernet) + Low-level primitives (RSA, AES)
C) UI

**Answer:** B

***

**Question 89** (🟡 Medium)

**Replay Attack Prevention**:
When implementing a protocol, always include:
A) A timestamp or nonce
B) Nothing
C) Encryption only

**Answer:** A

**Explanation:**
✅ **Nonce/Timestamp** prevents attacker from re-sending old message.

***

**Question 90** (🔴 Hard)

**Side-Channel Attacks**:
Python implementations are often vulnerable to:
A) Buffer overflows
B) Timing attacks (due to interpreter nature)
C) Pointers

**Answer:** B

**Explanation:**
✅ Python code execution time varies, making **timing attacks** easier than in C.

***

**Question 91** (🟡 Medium)

**Storing Passwords**:
Only store:
A) Plaintext
B) Encrypted password
C) Salted + Slow Hashed (e.g. Argon2, bcrypt)

**Answer:** C

***

**Question 92** (🟡 Medium)

**Encoding**:
`b = s.encode('utf-8')`
`s = b.decode('utf-8')`
If you decode random bytes as utf-8, it might:
A) Always work
B) Raise `UnicodeDecodeError`
C) Crash

**Answer:** B

**Explanation:**
✅ **Not all byte sequences are valid UTF-8**. Use `errors='ignore'` or base64 for random data.

***

**Question 93** (🟡 Medium)

**Eval()** function in Python:
A) Safe to use on user input
B) EXTREMELY DANGEROUS (RCE vulnerability)
C) Fast

**Answer:** B

**Explanation:**
✅ **Never `eval()` user input**. Can execute arbitrary code.

***

**Question 94** (🟡 Medium)

**Assert** statements (`assert x > 0`):
Uses:
A) Security checks
B) Debugging only (can be optimized out with `-O` flag)
C) Production logic

**Answer:** B

**Explanation:**
✅ **Don't use assert for security**; it's disabled in optimized mode.

***

**Question 95** (🟡 Medium)

**Pickle** module (serialization):
A) Safe for untrusted data
B) Insecure (unpickling can execute code)
C) JSON equivalent

**Answer:** B

**Explanation:**
✅ **Pickle is insecure**. Use JSON for untrusted data using `json` module.

***

**Question 96** (🟡 Medium)

**JSON** (`import json`):
A) Can handle bytes
B) Can only handle text/numbers/lists/dicts (no bytes)
C) Encrypts data

**Answer:** B

**Explanation:**
✅ **JSON doesn't support bytes**. Must Base64 encode bytes first.

***

**Question 97** (🟡 Medium)

**Why use `with` statement?**
`with open("key.pem", "rb") as f:`
A) Handles file closing automatically (even if error occurs)
B) It's faster
C) No reason

**Answer:** A

**Explanation:**
✅ **Resource management**. Prevents file handle leaks.

***

**Question 98** (🟡 Medium)

**Type Hinting** (`def func(a: int) -> str:`):
A) Enforces types at runtime
B) Documentation/IDE help only (Python ignores it)
C) Makes it static

**Answer:** B

**Explanation:**
✅ Python is **dynamic**. Type hints are for developers/tools only.

***

**Question 99** (🟡 Medium)

**Virtual Environments** (`venv`):
Why use them?
A) To isolate dependencies for each project
B) To make computer faster
C) Required by law

**Answer:** A

**Explanation:**
✅ **Dependency Isolation**. Avoids version conflicts.

***

**Question 100** (⚪ Easy)

**Best Practice:**
"Roll your own crypto" vs "Don't roll your own crypto".
**Answer:** **Don't roll your own crypto.**
Use standard, vetted libraries (`cryptography`, `nacl`, `openssl`).

***

## 📊 **COMPLETE BREAKDOWN**

**Total Questions:** 100

**Breakdown:**

- Python vs JavaScript Basics: 30 questions
- Python Crypto Libraries: 30 questions
- RSA in Python: 20 questions
- Practical Applications: 20 questions

***

## 🎯 **KEY EXAM TIPS**

### **⚡ PYTHON VS JAVASCRIPT - KEY DIFFERENCES:**

| Feature | Python | JavaScript |
| :-- | :-- | :-- |
| **Naming** | lowercase_with_underscores | camelCase |
| **Variables** | x = 1 | var x = 1; let x = 1; |
| **Constants** | ALLCAPS (convention) | const VAT = 0.21; |
| **Line end** | Newline | Semicolon ; |
| **Comments** | \# or """ """ | // or /* */ |
| **Integer div** | // | Math.floor() |
| **Exponent** | ** | Math.pow() |
| **Equality** | == | === |
| **Logical** | and, or, not | \&\&, \|\|, ! |
| **Code blocks** | Indentation | Curly braces {} |
| **Input** | input() | window.prompt() |
| **Output** | print() | console.log() |

### **📝 PYTHON CRYPTO LIBRARIES:**

**Installation:**

```bash
pip install base64
pip install hashlib
pip install des
pip install cryptography
```

**Common patterns:**

1. **Base64:** `.encode('ascii')` → `b64encode()` → `.decode('ascii')`
2. **Hashing:** `hashlib.md5(b'...')` → `.hexdigest()`
3. **Salting:** Add `os.urandom(40)` before hashing
4. **DES:** `DesKey(b'8BYTEKEY')` → `.decrypt()`
5. **RSA:** Generate → Convert to PEM → Load from PEM → Encrypt/Decrypt

### **🔧 IMPORTANT FUNCTIONS:**

- `ord(char)` → ASCII value (A=65)
- `chr(value)` → char from ASCII
- `.encode('ascii')` → string to bytes
- `.decode('ascii')` → bytes to string
- `.hexdigest()` → hash as hex string
- `gmpy2.powmod(base, exp, mod)` → modular exponentiation

***

## 🎓 **GRAND TOTAL: 1,630+ QUESTIONS!**

✅ Module 1 - Concepts I: 160 questions
✅ Module 1 - Concepts II: 210 questions
✅ Module 2 - Policies: 180 questions
✅ Module 3 - Encoding \& Modulo: 120 questions
✅ Module 3 - Information Flow: 200 questions
✅ Module 4 - Encryption \& Hashing: 200 questions
✅ Module 5 - Math of Cryptography: 260 questions
✅ Module 5 - Scripting for Cryptography: 100 questions

**YOU'RE UNSTOPPABLE, Champ!** 🔥🚀

You now have comprehensive coverage of **Python vs JavaScript** for cryptography with **practical implementations**! Ready for more modules? 💪
